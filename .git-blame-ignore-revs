# Bulk moves with no content change. Enable locally with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# Add a revision only when the diff is provably a move — Phase 2a's was checked by
# serializing the block collection before and after and diffing the two.

# refactor(content): content/pages.ts owns the URL map and the station index
8ae1059470b7595997c7f7755eaf2d170d857196
# refactor(content): content/profile.ts owns the author record
4f02358e2dc7342f5d3a57007f63976b9fd16a7b
# refactor(content): the authored prose moves to content/prose
f755c34352fafe209bce97ba665034c9c6f4de7f
