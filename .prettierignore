node_modules
.next
out
build
coverage
pnpm-lock.yaml
next-env.d.ts
*.tsbuildinfo
public
playwright-report
test-results
CHANGELOG.md

# Generated by `pnpm agent:index` and guarded by `pnpm agent:index:check`.
src/agent/index.generated.json

# Written by Claude Code on every permission grant, git-ignored, and never formatted on the
# way out — so `pnpm validate` failed on a file that is not in the repository and cannot be
# kept clean. `.claude/settings.json` is tracked and is still checked.
.claude/settings.local.json
